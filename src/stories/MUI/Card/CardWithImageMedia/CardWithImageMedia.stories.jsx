import React from 'react';

import { CardWithImageMedia } from './CardWithImageMedia';

export default {
  title: 'MUI/Card/CardWithImageMedia',
  component: CardWithImageMedia,
};

const Template = (args) => <CardWithImageMedia {...args} />;

export const Default = Template.bind({});

Default.args = {
  
};