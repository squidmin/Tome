import React from 'react';

import { CardOutlined } from './CardOutlined';

export default {
  title: 'MUI/Card/CardOutlined',
  component: CardOutlined,
};

const Template = (args) => <CardOutlined {...args} />;

export const Default = Template.bind({});

Default.args = {
  
};