import React from 'react';

import { CardBasic } from './CardBasic';

export default {
  title: 'MUI/Card/CardBasic',
  component: CardBasic,
};

const Template = (args) => <CardBasic {...args} />;

export const Default = Template.bind({});

Default.args = {
  
};