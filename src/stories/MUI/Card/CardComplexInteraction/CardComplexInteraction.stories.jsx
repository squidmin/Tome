import React from 'react';

import { CardComplexInteraction } from './CardComplexInteraction';

export default {
  title: 'MUI/Card/CardComplexInteraction',
  component: CardComplexInteraction,
};

const Template = (args) => <CardComplexInteraction {...args} />;

export const Default = Template.bind({});

Default.args = {
  
};