import React from 'react';

import { CardUIControls } from './CardUIControls';

export default {
  title: 'MUI/Card/CardUIControls',
  component: CardUIControls,
};

const Template = (args) => <CardUIControls {...args} />;

export const Default = Template.bind({});

Default.args = {
  
};