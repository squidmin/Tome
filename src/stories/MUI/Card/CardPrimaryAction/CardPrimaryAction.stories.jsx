import React from 'react';

import { CardPrimaryAction } from './CardPrimaryAction';

export default {
  title: 'MUI/Card/CardPrimaryAction',
  component: CardPrimaryAction,
};

const Template = (args) => <CardPrimaryAction {...args} />;

export const Default = Template.bind({});

Default.args = {
  
};