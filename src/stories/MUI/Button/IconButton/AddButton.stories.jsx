import React from 'react';

import { AddButton } from './AddButton';


export default {
  title: 'MUI/Button/AddButton',
  component: AddButton,
};

const Template = (args) => <AddButton {...args} />


export const Default = Template.bind({});
Default.args = {
};