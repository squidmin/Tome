import React from 'react';

import { AppBarDense } from './AppBarDense';

export default {
  title: 'MUI/AppBar/AppBarDense',
  component: AppBarDense,
};

const Template = (args) => <AppBarDense {...args} />;

export const Default = Template.bind({});

Default.args = {
  
};