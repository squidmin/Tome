import React from 'react';

import { AppBarResponsiveWithDrawer } from './AppBarResponsiveWithDrawer';

export default {
  title: 'MUI/AppBar/AppBarResponsiveWithDrawer',
  component: AppBarResponsiveWithDrawer,
};

const Template = (args) => <AppBarResponsiveWithDrawer {...args} />

export const Default = Template.bind({});

Default.args = {
  
};