import React from 'react';

import { AppBarBackToTop } from './AppBarBackToTop';

export default {
  title: 'MUI/AppBar/AppBarBackToTop',
  component: AppBarBackToTop,
};

const Template = (args) => <AppBarBackToTop {...args} />;

export const Default = Template.bind({});

Default.args = {
  
};