import React from 'react';

import { AppBarWithMenu } from './AppBarWithMenu';

export default {
  title: 'MUI/AppBar/AppBarWithMenu',
  component: AppBarWithMenu,
};

const Template = (args) => <AppBarWithMenu {...args} />;

export const Default = Template.bind({});

Default.args = {
  
};