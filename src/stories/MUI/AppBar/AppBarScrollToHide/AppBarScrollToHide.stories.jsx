import React from 'react';

import { AppBarScrollToHide } from './AppBarScrollToHide';

export default {
  title: 'MUI/AppBar/AppBarScrollToHide',
  component: AppBarScrollToHide,
};

const Template = (args) => <AppBarScrollToHide {...args} />;

export const Default = Template.bind({});

Default.args = {
  
};