import React from 'react';

import { AppBarBottom } from './AppBarBottom';

export default {
  title: 'MUI/AppBar/AppBarBottom',
  component: AppBarBottom,
};

const Template = (args) => <AppBarBottom {...args} />;

export const Default = Template.bind({});

Default.args = {
  
};