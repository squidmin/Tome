import React from 'react';

import { AppBarScrollToElevate } from './AppBarScrollToElevate';

export default {
  title: 'MUI/AppBar/AppBarScrollToElevate',
  component: AppBarScrollToElevate,
};

const Template = (args) => <AppBarScrollToElevate {...args} />;

export const Default = Template.bind({});

Default.args = {
  
};