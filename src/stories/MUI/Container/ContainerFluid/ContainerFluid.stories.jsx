import React from 'react';

import { ContainerFluid } from './ContainerFluid';

export default {
  title: 'MUI/Container/ContainerFluid',
  component: ContainerFluid,
};

const Template = (args) => <ContainerFluid {...args} />;

export const Default = Template.bind({});

Default.args = {
  
};