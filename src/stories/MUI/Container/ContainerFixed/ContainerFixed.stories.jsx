import React from 'react';

import { ContainerFixed } from './ContainerFixed';

export default {
  title: 'MUI/Container/ContainerFixed',
  component: ContainerFixed,
};

const Template = (args) => <ContainerFixed {...args} />;

export const Default = Template.bind({});

Default.args = {
  
};