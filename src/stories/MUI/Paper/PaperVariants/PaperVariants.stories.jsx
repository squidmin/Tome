import React from 'react';

import { PaperVariants } from './PaperVariants';

export default {
  title: 'MUI/Paper/PaperVariants',
  component: PaperVariants,
};

const Template = (args) => <PaperVariants {...args} />;

export const Default = Template.bind({});

Default.args = {
  
};