import React from 'react';

import { PaperBasic } from './PaperBasic';

export default {
  title: 'MUI/Paper/PaperBasic',
  component: PaperBasic,
};

const Template = (args) => <PaperBasic {...args} />;

export const Default = Template.bind({});

Default.args = {
  
};