import React from 'react';

import { BoxBasic } from './BoxBasic';

export default {
  title: 'MUI/Box/BoxBasic',
  component: BoxBasic,
};

const Template = (args) => <BoxBasic {...args} />;

export const Default = Template.bind({});

Default.args = {
  
};