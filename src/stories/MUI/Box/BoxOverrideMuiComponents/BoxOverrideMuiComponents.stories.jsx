import React from 'react';

import { BoxOverrideMuiComponents } from './BoxOverrideMuiComponents';

export default {
  title: 'MUI/Box/BoxOverrideMuiComponents',
  component: BoxOverrideMuiComponents,
};

const Template = (args) => <BoxOverrideMuiComponents {...args} />;

export const Default = Template.bind({});

Default.args = {
  
};